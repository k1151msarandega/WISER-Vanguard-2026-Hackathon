# Portfolio Co-Pilot Demo (Week 3-4)

Placeholder. This will be the interactive artifact required by the brief:
shows the recommended allocation, why each asset is in H(old)/O(ptimize)/S(kip),
and what happens when the growth/income/drawdown/cost dials move.

Planned stack: Streamlit (installable via `pip install -e ".[app]"`), reading
results produced by `vqportfolio.partitioning` and `vqportfolio.quantum` once
those exist (Week 2).

Nothing here yet — don't build against this until the partitioning + QUBO
modules are in place.
